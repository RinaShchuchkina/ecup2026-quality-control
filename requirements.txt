# Python 3.12
#
# --- Уже есть в среде исполнения платформы -----------------------------------
# Версии зафиксированы по боевому образу. Доустанавливать не требуется,
# список приведён для воспроизведения окружения локально.
torch==2.10.0
torchvision==0.25.0
transformers==5.14.1
safetensors==0.8.0
scikit-learn==1.9.0
numpy==2.2.3
pandas==2.2.3
scipy==1.18.0
Pillow==11.1.0

# --- Нужно доустановить (нет в образе платформы) ------------------------------
# peft и timm вендорятся в архив сабмита: без них не поднимаются
# LoRA-адаптеры в src/predict.py и EVA-бранч в src/eva_branch.py.
peft==0.20.0
timm==1.0.27

# --- Только для обучения ------------------------------------------------------
# Требуется одному src/train_lora.py (process_vision_info при сборке батча).
# На инференсе не нужен: src/vision.py реализует разбор картинок самостоятельно.
qwen-vl-utils
